interface:
  display_name: "Git Team Workflow"
  short_description: "Guide branch, push, PR, merge, and cleanup flow"
  default_prompt: "Use $git-collaboration-flow to guide the standard team Git workflow from branching off main through PR and post-merge cleanup."
